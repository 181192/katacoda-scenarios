configMapGenerator:
  - name: ingress-nginx-controller-dashboard
    files:
      - ingress-nginx-controller.json

generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"
